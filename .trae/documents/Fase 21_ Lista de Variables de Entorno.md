# Fase 21: Nombres de Variables de Entorno (Detallado) 📝

El usuario pregunta qué nombre ponerle a las variables.
Voy a listar las esenciales para que no haya error.

## Nombres Exactos (Copiar y Pegar)
1.  **`GEMINI_API_KEY`**: Para la IA de Google.
2.  **`STRIPE_SECRET_KEY`**: Para los pagos.
3.  **`MONGO_URI`**: Para la base de datos (opcional si usa SQLite local, pero en Render SQLite se borra al reiniciar, así que Mongo es recomendado).
4.  **`FLASK_ENV`**: Poner `production`.

Le daré la lista en formato claro para que solo copie el nombre de la izquierda y pegue su clave a la derecha.

¿Te paso la lista para copiar? 📋